version: "3.8"

services:
  lamp:
    image: fauria/lamp
    container_name: lamp
    ports:
      - "8080:80"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: mydb
      MYSQL_USER: user
      MYSQL_PASSWORD: pass
    volumes:
      - ./www:/var/www/html